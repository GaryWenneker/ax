[package]
name = "ax-smoke-tests"
version = "2.0.8"
edition = "2021"
publish = false

[dev-dependencies]
ax-core = { path = "../ax-core" }
ax-extraction = { path = "../ax-extraction" }
ax-types = { path = "../ax-types" }
tokio = { workspace = true }
