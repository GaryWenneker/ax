---
id: react-hooks
level: HIGH
alwaysApply: false
globs: []
triggers: ["react", "hooks", "useEffect"]
tags: ["react"]
priority: 70
enabled: true
status: approved
scope: project
share: true
---

# React hooks

- Call hooks at the top level, unconditionally.
- Effect dependencies match the values the effect reads.
- Do not use an effect to copy props into state or to compute derived data.
