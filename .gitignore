# Rust
/target/
/target-dev/
/target-ui/
/target-linux/
/target-macos/
/target-release/
**/*.rs.bk

# Release packaging
/dist/
/dist-ci/
/release/npm/
install-out.txt

# Local project config (machine-specific)
ax.json

# Local cross-build helpers (machine-specific paths)
build-macos.sh
check-macos-error.sh

# Local MCP config (machine-specific paths)
.mcp.json

# ax project data (local indexes) — shareable policy is `.agents/`;
# `.ax/policy/pending` and `.ax/policy/shared` remain committable.
.ax/
!.ax/policy/
!.ax/policy/**

# SQLite
*.db
*.db-wal
*.db-shm

# Node (site + telemetry-worker)
node_modules/
site/dist/
telemetry-worker/.wrangler/

# IDE / OS
.idea/
.vscode/
# Seeded policy skills (ax init/install copies templates here — not repo content)
.cursor/skills/*
!.cursor/skills/ax-reinstall/
.DS_Store
Thumbs.db

# Environment / secrets
.env
.env.local
.env.*.local

# Logs
*.log

# TypeScript
*.tsbuildinfo

# Local Netlify folder
.netlify
site/deploy-*.zip

# Release binaries mirrored on getax.wenneker.io (CI deploy — do not commit zips)
site/public/releases/*/ax-*
site/public/releases/*/*.zip
site/public/releases/*/*.tar.gz
site/public/releases/*/SHA256SUMS
dist-ci-new/
dist-test/

# Smoke-test local indexes
test-smoke/.ax/
test-smoke-go/.ax/
test-smoke-rust/.ax/

# Accidental cargo target under web-ui
crates/ax-web/web-ui/target-dev/
crates/ax-web/web-ui/target/

# Playwright (Command Center mobile smoke)
crates/ax-web/web-ui/test-results/
crates/ax-web/web-ui/playwright-report/
crates/ax-web/web-ui/blob-report/

